# Binaries (anchored to repo root — does NOT exclude cmd/rms/, cmd/subcrawler/, …)
/rms
/rms-*
/subcrawler
/metacrawler
*.exe

# Media test files (anchored to repo root — does NOT exclude internal/media/)
/media/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# CONFIG
config/config.yml

# TV / IPTV playlists — user-provided; not shipped (third-party content)
/lists/